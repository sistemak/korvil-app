name: META IA PERMANENTE - sistemak/korvil-app
on:
  workflow_dispatch:
  schedule:
    - cron: '*/30 * * * *'

jobs:
  meta-ia:
    runs-on: ubuntu-latest
    steps:
      - name: IA PERMANENTE ATIVA
        run: |
          echo "IA ATIVA - $(date)"
          echo "sistemak - korvil-app - PERMANENTE"
          sleep 5
          echo "✅ IA ATIVA PRA SEMPRE"
